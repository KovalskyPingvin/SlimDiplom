{# templates/components/flash_message.twig #}
{% if session.flash_message is defined %}
    {% set msg = session.flash_message %}
    <div class="flash-message flash-{{ msg.type }}">
        {{ msg.message|e }}
    </div>
{% endif %}